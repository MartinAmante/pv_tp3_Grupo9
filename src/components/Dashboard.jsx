const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard</h2>
            <p>Bienvenido al sistema de gestión de proyectos.</p>
        </div>
    );
};

export default Dashboard;